using UnityEngine;
using Zenject;

public class HammerBuilder : IBuilder {

    public void SpawnForTile (Tile tile) {
		// HammerView hammer = _hammerFactory.Create();

		// // Vector3 pos = tile.transform.position;
		// Vector3 pos = Vector3.zero;
		// Vector3 tSize = tile.meshCollider.bounds.size;
		// pos.y = hammer.transform.position.y;
		// pos.z += (UnityEngine.Random.Range(-0.25f, 0.25f) * tSize.z);
		
		// hammer.transform.parent = tile.transform;
        // hammer.transform.position = pos;
    }
}